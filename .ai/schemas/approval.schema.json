{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ai-author-orchestration.dev/schemas/approval.schema.json",
  "title": "Human Approval Record",
  "type": "object",
  "required": [
    "schema_version",
    "approval_id",
    "artifact_type",
    "source_artifact",
    "artifact_hash",
    "decision",
    "approved_at",
    "approved_by"
  ],
  "properties": {
    "schema_version": {
      "type": "string"
    },
    "approval_id": {
      "type": "string",
      "pattern": "^apr-[0-9]{6,}$"
    },
    "artifact_type": {
      "enum": [
        "premise",
        "story_bible",
        "macro_outline",
        "act_outline",
        "chapter",
        "scene",
        "canon_retcon",
        "canon_proposal",
        "other"
      ]
    },
    "source_artifact": {
      "type": "string",
      "description": "Book-relative path whose bytes were hashed at approval time (usually a draft)"
    },
    "promotion_target": {
      "type": "string",
      "description": "Optional book-relative path for post-approval promotion; omit or equal source when no promotion"
    },
    "artifact_hash": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$",
      "description": "SHA-256 of source_artifact bytes at approval time"
    },
    "decision": {
      "enum": [
        "approved",
        "rejected"
      ]
    },
    "approved_at": {
      "type": "string"
    },
    "approved_by": {
      "const": "human"
    },
    "revision": {
      "type": "integer",
      "minimum": 1
    },
    "notes": {
      "type": "string"
    }
  },
  "additionalProperties": false
}
