# Book workspaces

Active novels and novellas live here as `books/<slug>/`.

The framework example book lives at `.ai/examples/books/memory-echo/` and is **not** an active book for resolution.

Create a new book with the `orchestration/initialize-book` protocol — do not copy the example into `books/` unless you intend it as a real project (and then rename the slug).
